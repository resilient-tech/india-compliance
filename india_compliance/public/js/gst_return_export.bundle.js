// Loaded on demand by the GST Return Export tool (via frappe.require) so the summary
// can render with the same native components as the reconciliation tools: the
// DataTable (section table) and the number-card strip (ITC).
import "./components/data_table_manager";
import "./components/number_card";
